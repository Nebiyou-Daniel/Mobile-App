import 'package:frontend/Reviews/model/review_model.dart';

class ApiDataProvidor {
// getAllReviews(trainerId: event.trainerId, accessToken: accessToken)
  getAllReviews({required int trainerId, required String accessToken}) {
    // TODO: implement getAllReviews
  }

  // createReview(review: event.review, accessToken: accessToken);
  createReview({required Review review, required String accessToken}) {
    // TODO: implement createReview
  }

  // get the review written by the current user identified by the accesstoken to the 
  // current trainer identified by the trainerId
  


}
